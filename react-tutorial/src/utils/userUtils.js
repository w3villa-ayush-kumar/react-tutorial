export const getUserInfo = ({ name, role }) => {
    return `User: ${name}, Role: ${role}`;
};
// const getUserInfo = ({ name, role }) => {
//     return `User: ${name}, Role: ${role}`;
// };
// module.exports = {
//     getUserInfo
// }